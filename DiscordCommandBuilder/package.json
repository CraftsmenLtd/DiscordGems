{
  "name": "discord-command-builder",
  "version": "1.0.0",
  "description": "A program to register commands to a discord bot that has aws lambda as backend",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.3.3"
  }
}
